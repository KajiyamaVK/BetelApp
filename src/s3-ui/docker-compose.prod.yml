services:
  app:
    build:
      context: .
      target: runner
    ports:
      - "3001:3000"
    env_file:
      - .env.production
    restart: unless-stopped

  # Run with: docker compose --profile migrate run --rm migrator
  migrator:
    build:
      context: .
      target: migrator
    env_file:
      - .env.production
    profiles:
      - migrate
